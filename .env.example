# =================================================================
# 核心说明：请复制此文件并重命名为 .env，然后填写实际参数。
# 不要将包含真实数据的 .env 文件提交到 Git 仓库！
# =================================================================

# Nest 服务配置
nest_server_port=3000
cors_origin=*

# Redis 相关配置
# 在 Docker Compose 环境下，HOST 填写容器服务名：redis-container
redis_server_host=redis-container
redis_server_port=6379
redis_server_db=1 

# Nodemailer 相关配置 (邮件服务)
# host: 例如 smtp.qq.com
nodemailer_host=smtp.qq.com
nodemailer_port=587
nodemailer_auth_user=your_email@example.com
# auth_pass: 邮件服务的授权码，而非登录密码
nodemailer_auth_pass=your_email_auth_code

# MySQL 相关配置
# 在 Docker Compose 环境下，HOST 填写容器服务名：mysql-container
mysql_server_host=mysql-container
mysql_server_port=3306
mysql_server_username=root
mysql_server_password=your_database_password
mysql_server_database=meeting_room_booking_system
mysql_server_synchronize=true


# JWT 配置
# jwt_secret: 用于加密 Token 的随机字符串，建议设置得复杂一些
jwt_secret=your_jwt_secret_key
jwt_access_token_expires_time=30m
jwt_refresh_token_expres_time=7d

# Minio 配置 (对象存储)
# 如果 Minio 也跑在 Docker 里，这里的 localhost 可能需要改为 minio-container
minio_endpoint=localhost
minio_port=9000
minio_access_key=your_minio_access_key
minio_secret_key=your_minio_secret_key